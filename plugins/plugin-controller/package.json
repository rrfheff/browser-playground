{
  "name": "@browser-playground/plugin-controller",
  "version": "0.0.1",
  "description": "Plugin controller package for @browser-playground/core (example plugins).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:rrfheff/browser-playground.git",
    "directory": "plugins/plugin-controller"
  },
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsup src/index.ts --format esm,cjs --dts --watch",
    "lint": "echo 'lint not configured for plugin-controller'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@browser-playground/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.2.0"
  }
}
